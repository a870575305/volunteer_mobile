<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="5dp"
    android:paddingBottom="5dp">
    <ImageView
        android:id="@+id/list_item_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="26dp"
        app:srcCompat="@mipmap/ca_medical" />

    <TextView
        android:id="@+id/list_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/list_item_icon"
        android:layout_marginStart="22dp"
        android:layout_toEndOf="@+id/list_item_icon"
        android:text="東京湾を泳げるようにするイベント" />

    <TextView
        android:id="@+id/list_item_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/list_item_icon"
        android:layout_alignStart="@+id/list_item_title"
        android:text="@string/top_list_item_time" />

    <TextView
        android:id="@+id/list_item_place"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/list_item_icon"
        android:layout_alignParentEnd="true"
        android:textColor="@color/Gray"
        android:text="東京都" />
</RelativeLayout>